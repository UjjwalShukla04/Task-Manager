# REST API base URL (includes the /api path).
VITE_API_URL="http://localhost:5000/api"

# Optional: Socket.io origin. Defaults to VITE_API_URL without the /api suffix.
# VITE_SOCKET_URL="http://localhost:5000"
